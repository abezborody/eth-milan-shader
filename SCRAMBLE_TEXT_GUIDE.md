# Гайд по использованию Scramble Text в Webflow

## Что это?

Эффект "Scramble Text" создает анимацию, при которой текст появляется через случайные символы (как в хакерских фильмах). Буквы быстро меняются, пока не превратятся в нужный текст.

## Установка

### Способ 1: Через Embed Code (рекомендуется)

1. Откройте ваш проект в Webflow
2. Добавьте элемент **Embed** на страницу (найдите в панели "Components")
3. Вставьте следующий код:

```html
<script src="https://your-domain.com/scramble-text.js"></script>

<script>
  // Инициализация для элементов с классом "scramble-text"
  document.addEventListener('DOMContentLoaded', function() {
    const scramblers = [];

    document.querySelectorAll('.scramble-text').forEach(function(el) {
      const scrambler = new ScrambleText(el, {
        text: el.textContent,
        speed: 1,
        scramble: 2
      });
      scramblers.push(scrambler);
    });

    // Если нужно переиграть анимацию (например, при скролле)
    window.replayScramble = function() {
      scramblers.forEach(function(s) { s.play(); });
    };
  });
</script>
```

### Способ 2: Непосредственно в Embed

Вставьте весь код класса и инициализацию в один Embed элемент:

```html
<script>
// Вставьте сюда весь код класса ScrambleText...
// (из файла useScrambleText.js)

// Затем инициализируйте:
document.addEventListener('DOMContentLoaded', function() {
  new ScrambleText('.my-heading', {
    text: 'Привет, мир!',
    speed: 1
  });
});
</script>
```

## Базовое использование

### Шаг 1: Добавьте текст в Webflow

1. Добавьте **Text Element** или **Heading** на вашу страницу
2. Задайте ему класс (например, `scramble-text` или любой другой)
3. Напишите ваш текст

### Шаг 2: Инициализация с базовыми настройками

```javascript
new ScrambleText('.your-class-name');
```

### Шаг 3: Настройка анимации через опции

```javascript
new ScrambleText('.your-class-name', {
  text: 'Ваш текст здесь',
  speed: 1,        // Скорость (0-1)
  scramble: 2,     // Сколько раз менять каждый символ
  tick: 1          // Кадров между обновлениями
});
```

## Параметры настройки

| Параметр | Тип | По умолчанию | Описание |
|----------|-----|--------------|----------|
| `text` | string | `''` | Текст для анимации (если не указан, берется из элемента) |
| `speed` | number | `1` | Скорость анимации (0-1). 1 = 60 кадров в секунду |
| `scramble` | number | `1` | Сколько раз "перемешивать" каждый символ |
| `tick` | number | `1` | Обновлять каждые N кадров |
| `step` | number | `1` | Сколько символов добавлять за раз |
| `seed` | number | `1` | Сколько случайных символов добавлять вперед |
| `chance` | number | `1` | Верояттность перемешивания (0-1) |
| `range` | array | `[65, 125]` | Диапазон Unicode символов для перемешивания |
| `overflow` | boolean | `true` | Начинать с пустой строки |
| `overdrive` | boolean/number | `true` | Режим "overdrive" с специальным символом |
| `ignore` | array | `[' ']` | Символы, которые не перемешиваются |
| `playOnMount` | boolean | `true` | Играть ли при загрузке |

## Примеры

### Пример 1: Медленная анимация заголовка

```javascript
new ScrambleText('h1', {
  speed: 0.3,        // Медленная скорость
  scramble: 3,       // Больше перемешиваний
  range: [65, 90]    // Только заглавные буквы (A-Z)
});
```

### Пример 2: Быстрый эффект "матрицы"

```javascript
new ScrambleText('.matrix-text', {
  text: 'THE MATRIX',
  speed: 1,          // Максимальная скорость
  scramble: 5,       // Много перемешиваний
  tick: 1,
  range: [33, 126]   // Все печатные символы
});
```

### Пример 3: Сохранять пробелы и знаки препинания

```javascript
new ScrambleText('.preserve-spaces', {
  text: 'Hello, World!',
  ignore: [' ', ',', '!', '.'],  // Не перемешивать эти символы
  scramble: 2
});
```

### Пример 4: Кастомный Unicode диапазон (только цифры)

```javascript
new ScrambleText('.numbers-only', {
  text: '12345',
  range: [48, 57]    // Цифры 0-9
});
```

### Пример 5: Не проигрывать при загрузке

```javascript
const scrambler = new ScrambleText('.hover-effect', {
  text: 'Наведи на меня',
  playOnMount: false  // Не проигрывать автоматически
});

// Проиграть при наведении курсора
document.querySelector('.hover-effect').addEventListener('mouseenter', function() {
  scrambler.play();
});
```

## Продвинутое использование

### Анимация при скролле (Scroll Trigger)

```javascript
// Используйте Webflow Interactions или библиотеку вроде Intersection Observer
const observer = new IntersectionObserver(function(entries) {
  entries.forEach(function(entry) {
    if (entry.isIntersecting) {
      const scrambler = new ScrambleText(entry.target, {
        text: entry.target.textContent,
        speed: 0.8
      });
      observer.unobserve(entry.target);
    }
  });
});

document.querySelectorAll('.scramble-on-scroll').forEach(function(el) {
  observer.observe(el);
});
```

### Анимация по клику

```javascript
const buttonScrambler = new ScrambleText('.button-text', {
  text: 'Нажми меня',
  playOnMount: true
});

document.querySelector('.my-button').addEventListener('click', function() {
  buttonScrambler.updateText('Отлично!');
  buttonScrambler.play();
});
```

### Использование с колбэками

```javascript
new ScrambleText('.callback-demo', {
  text: 'Анимация запущена...',
  onAnimationStart: function() {
    console.log('Анимация началась!');
  },
  onAnimationFrame: function(result) {
    console.log('Текущий текст:', result);
  },
  onAnimationEnd: function() {
    console.log('Анимация завершена!');
    // Запустить следующую анимацию
    new ScrambleText('.next-element', {
      text: 'Следующий текст'
    });
  }
});
```

### Несколько элементов с разными настройками

```javascript
// Быстрый для заголовка
new ScrambleText('h1', {
  speed: 1,
  scramble: 3
});

// Медленный для подзаголовка
new ScrambleText('h2', {
  speed: 0.5,
  scramble: 1
});

// Средний для параграфа
new ScrambleText('p', {
  speed: 0.7,
  scramble: 2,
  ignore: [' ', ',', '.', '!', '?']
});
```

## Методы класса

### `.play()`
Запускает анимацию заново

```javascript
scrambler.play();
```

### `.updateText(newText)`
Обновляет текст для анимации

```javascript
scrambler.updateText('Новый текст');
scrambler.play();  // Запустить с новым текстом
```

### `.destroy()`
Останавливает анимацию и очищает ресурсы

```javascript
scrambler.destroy();
```

## Unicode диапазоны

Популярные диапазоны для параметра `range`:

- `[65, 90]` - Заглавные буквы (A-Z)
- `[97, 122]` - Строчные буквы (a-z)
- `[48, 57]` - Цифры (0-9)
- `[33, 126]` - Все печатные ASCII символы
- `[1040, 1071]` - Заглавные русские буквы (А-Я)
- `[1072, 1103]` - Строчные русские буквы (а-я)

### Пример с русскими буквами

```javascript
new ScrambleText('.russian-text', {
  text: 'Привет, мир!',
  range: [1040, 1103],  // Все русские буквы
  ignore: [' ', ',', '!']
});
```

## Советы по производительности

1. **Не анимируйте слишком много элементов одновременно** - это может вызвать лаги
2. **Используйте `playOnMount: false`** для элементов, которые не видны сразу
3. **Настройте `speed`** для баланса между эффектом и производительностью
4. **Используйте Intersection Observer** для запуска анимации только когда элемент виден

## Доступность (Accessibility)

Скрипт автоматически учитывает предпочтение пользователя `prefers-reduced-motion`:

```javascript
// Если пользователь включил "снижение движения" в системе,
// анимация будет проиграна мгновенно без эффекта перемешивания
```

## Полный пример с комментариями

```javascript
document.addEventListener('DOMContentLoaded', function() {
  // Создаем эффект для главного заголовка
  const heroScrambler = new ScrambleText('#hero-title', {
    text: 'Добро пожаловать в будущее',
    speed: 0.8,              // 80% от максимальной скорости
    scramble: 3,             // Каждый символ меняется 3 раза
    tick: 1,                 // Обновлять каждый кадр
    step: 2,                 // Добавлять по 2 символа за раз
    seed: 1,                 // Добавлять 1 случайный символ вперед
    chance: 1,               // 100% вероятность перемешивания
    range: [65, 125],        // Латинские буквы и символы
    overflow: true,          // Начинать с пустой строки
    ignore: [' '],           // Не перемешивать пробелы
    playOnMount: true,       // Проиграть при загрузке

    // Колбэки
    onAnimationStart: function() {
      console.log('Анимация началась');
    },
    onAnimationEnd: function() {
      console.log('Анимация завершена');

      // Показать кнопку после анимации
      document.querySelector('.cta-button').style.opacity = '1';
    }
  });

  // Проиграть заново при клике на заголовок
  document.querySelector('#hero-title').addEventListener('click', function() {
    heroScrambler.play();
  });
});
```

## Типичные проблемы и решения

### Проблема: Текст не анимируется
**Решение:** Убедитесь, что:
- Элемент существует на странице
- CSS selector правильный
- Скрипт загружен после DOM (используйте `DOMContentLoaded`)

### Проблема: Анимация слишком быстрая/медленная
**Решение:** Настройте параметр `speed`:
- `speed: 0.3` - медленно
- `speed: 1` - максимально быстро

### Проблема: Некоторые символы перемешиваются
**Решение:** Добавьте их в массив `ignore`:
```javascript
ignore: [' ', ',', '.', '!', '?', '-', '—']
```

### Проблема: Работает на одном элементе, но не на нескольких
**Решение:** Используйте `querySelectorAll` и создавайте отдельный инстанс для каждого:

```javascript
document.querySelectorAll('.scramble').forEach(function(el) {
  new ScrambleText(el, {
    text: el.textContent
  });
});
```

## Интеграция с Webflow Interactions

Вы можете комбинировать этот скрипт с нативными анимациями Webflow:

1. Создайте анимацию в Webflow для изменения opacity/scale
2. Используйте скрипт для эффекта перемешивания текста
3. Запускайте их одновременно

Пример:
```javascript
// Запустить при клике через Webflow interaction
window.startScrambleAnimation = function() {
  const scrambler = new ScrambleText('#animated-text', {
    text: 'Новый текст',
    speed: 1
  });
};
```

Затем в Webflow Interaction добавьте действие "Run JavaScript" и вызовите `startScrambleAnimation()`.

---

## Демо и идеи

- **Hero секция** - Большой заголовок с эффектом при загрузке
- **Кнопки** - Текст меняется при наведении
- **Счетчики** - Цифры "перемешиваются" при скролле
- **Навигация** - Эффект при смене активного пункта
- **Формы** - Placeholder текст с анимацией
- **Таймеры обратного отсчета** - Цифры перемешиваются каждую секунду

Удачи с вашими проектами в Webflow! 🚀
